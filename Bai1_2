// 1.2: tinh va xuat tong gia tri tung dong
void tinhTongDong(int a[][MAX], int m, int n) {
	for (int i = 0; i < m; i++) {
		int sum = 0;
		for (int j = 0; j < n; j++) {
			sum += a[i][j];
		}
		printf("Tong dong %d: %d\n", i, sum);
	}
}
